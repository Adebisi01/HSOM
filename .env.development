# Public Convex URLs (same deployment as default CONVEX_SITE_URL in api/auth)
PUBLIC_CONVEX_URL=https://efficient-swan-827.convex.cloud
PUBLIC_CONVEX_SITE_URL=https://efficient-swan-827.convex.site

# Replace with a real key from https://resend.com — placeholder only so the env module exports at build time
RESEND_API_KEY=re_replace_me
