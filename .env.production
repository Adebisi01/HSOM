# Public Convex URLs — override per environment on the host if needed
PUBLIC_CONVEX_URL=https://efficient-swan-827.convex.cloud
PUBLIC_CONVEX_SITE_URL=https://efficient-swan-827.convex.site

# Set a real key on the host (e.g. Vercel) — placeholder satisfies SvelteKit static env at build time
RESEND_API_KEY=re_replace_me
